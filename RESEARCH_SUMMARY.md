# 研究总结：LLM 驱动的多能源时序预测自进化特征工程 Agent

> 本文档是论文/报告的完整骨架。内容全部来自仓库真实代码 + 36 次真实 LLM 实验（qwen-max）+ 9 个测试套件全绿的验证结果。可据此直接展开成论文各章节。

---

## 一、研究背景

### 1.1 问题定义

多能源时序预测（负荷 / 光伏 / 风电 / 电价）是电力系统运行的核心基础——调度、储能、市场交易都依赖高质量的预测。预测精度的瓶颈往往不在模型，而在**特征工程**：

- 预测性能对特征敏感：滞后项、滚动统计、周期交互、气象外生的组合方式直接决定上限；
- 特征工程是高度**专家依赖 + 试错型**的工作：靠人力反复实验，难以复制、难以审计；
- **数据泄露风险**：特征构造一旦引入未来信息（如未 shift 的滚动窗口），验证分数虚高、上线即失效——这是时序领域最隐蔽的坑。

### 1.2 现有方法的局限

| 范式 | 代表 | 局限 |
|---|---|---|
| 手工特征 + 浅模型 | 梯度提升树 | 专家依赖、试错成本高、不可复现 |
| 端到端深模型 | Transformer / PatchTST | 特征不可解释、外生信息利用弱、数据需求大 |
| 传统 AutoML | 网格/贝叶斯搜索 | 特征空间搜索效率低、无领域知识注入、泄露防护弱 |
| LLM 直接预测 | GPT-based forecaster | 把 LLM 当预测器，舍本逐末——LLM 的价值在"决策"而非"数值回归" |

### 1.3 核心观察

> **LLM 的价值不在"预测数值"，而在"基于证据做特征工程决策"。** 让 LLM 像资深工程师一样，读数据统计 → 提假设 → 设计特征 → 看结果 → 记忆经验，是在不牺牲可解释性/安全性的前提下，把特征工程自动化的正确分工。

---

## 二、Related Works

### 2.1 自动化特征工程
- **经典 AutoML**（Auto-sklearn / TPOT）：进化的特征生成 + 模型搜索，但缺乏领域知识、泄露防护弱、搜索昂贵。
- **时序专用特征库**（tsfresh / Kats）：大规模自动特征提取，但产生数千冗余特征，需特征选择后处理，同样无因果防护。
- **LLM 特征工程**（LLM-based feature engineering，如 FEVA）：用 LLM 生成候选特征，但多数缺乏**受约束动作空间 + 无泄漏评测闭环**——这正是本工作的差异化。

### 2.2 LLM for Time Series
- **LLM 作为预测器**：Time-LLM / GPT4TS 等把时序 patch 化喂给 LLM，输出预测。我们主张 LLM 不应做数值回归（其概率解码不适配连续时序）。
- **LLM 作为 Agent**：ReAct / Tool-augmented 范式把 LLM 接工具执行。本工作把"工具"限定为**确定性特征引擎 + 无泄漏评测器**，形成决策闭环。

### 2.3 时序预测深模型
- **Transformer 系**（Informer / Autoformer / PatchTST）：端到端学习表示，是现有基线。我们在 ECL 跨用户迁移上用 PatchTST 作为对照，证明深模型适合"跨序列泛化"而 Agent 适合"单序列特征工程"。

### 2.4 决策聚焦预测（Decision-focused forecasting）
- 预测的最终价值在决策（储能套利、市场报价）。本工作在 V2.2 起把**储能套利利润 / Regret** 设为价值出口（P-Value），区别于纯 RMSE 评估。

---

## 三、Our Work 概览

我们构建了一个 **LLM 驱动的多能源时序预测自进化特征工程 Agent**，核心主张：

1. **LLM 是决策器，不是预测器**：在受约束动作空间内设计特征，从不写代码；
2. **受约束动作空间 + 白名单 = 安全网**：任何候选（包括被误导的）都无法越界或引入泄露；
3. **无泄漏闭环评测**：所有决策基于预测月 online_h1 滚动回放的 RMSE，可审计可复现；
4. **领域知识可插拔**：按数据集注入特征工程先验，效果可用对照实验量化；
5. **多能源泛化**：Load / Solar / Wind / Price 四赛道统一接入，跨数据集验证；
6. **风险感知扩展**：从点预测扩展到分位数输出（不确定性 Agent），面向电价尖峰场景。

---

## 四、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                     LLM 决策器（不写代码）                      │
│  读：数据统计 / ACF / 特征重要性 / 误差画像 / 历史经验 / 领域先验 │
│  出：≤3 个候选假设，每个 = 一组有序动作（ADD/REMOVE/REPLACE/…）  │
└──────────────┬──────────────────────────────────────────────┘
               ▼
┌─────────────────────────────────────────────────────────────┐
│              确定性特征引擎 + 无泄漏评测（安全执行）              │
│  build_features（lag/rolling/time/cross 血缘式）               │
│  → normalize_spec / validate_spec_list（白名单 + 因果静态检查） │
│  → 预测月 online_h1 滚动回放（无未来信息）                      │
│  → Selector：择优 / 自动回滚                                  │
└──────────────┬──────────────────────────────────────────────┘
               ▼
┌─────────────────────────────────────────────────────────────┐
│               经验记忆（跨 Task 共享）                         │
│  场景相似度检索（季节 + ACF + CV）→ 相似经验喂回 LLM            │
└─────────────────────────────────────────────────────────────┘
```

### 4.1 内循环（P1-A：单 Task 自进化）
感知（误差画像）→ 决策（LLM 候选）→ 执行（特征引擎）→ 评估（无泄漏回放）→ 反馈（择优/回滚）→ 记忆。

### 4.2 外循环（P1-B：跨 Task 迁移）
Task 间漂移检测（均值/方差/分位/ACF/残余五类信号）→ LLM 决策 继承/修改/重置 → warm-start 下一 Task。

### 4.3 三档动作空间（核心机制）
| 档位 | 名称 | 允许特征 | 定位 |
|---|---|---|---|
| 1 | 基本时序 | time + 目标列 lag | 最保守，纯时序 |
| 2 | 能源专有 | + 外生列 lag/rolling | 领域特征（气象） |
| 3（默认） | 组合类 | + cross | 给 LLM 最多空间 |

### 4.4 领域知识注册表（可插拔先验）
load / solar / wind / price / ecl 五段先验，按 `--dataset` 注入 prompt，`--domain-key` 可显式控制（含负对照）。

### 4.5 扩展子系统（队友 v2.2 合入）
- **模型选择 Agent**：LLM 每个候选可指定后端（lightgbm/lstm/persistence/…），评测缓存按 `(task, zone, spec, model)` 隔离；
- **风险感知不确定性 Agent**：分位数 / pinball / 保形校准，面向电价尖峰的概率输出；
- **Price 第四赛道**：肥尾电价 + 外生负荷特征 + P-Value 决策价值主线；
- **ECL 跨用户记忆对照**（Exp4）。

---

## 五、实验结果

> 全部结论来自**真实 LLM（qwen-max）多次运行**（每臂 3 seeds，`max_iter=5`，`patience=3`，cold_start 起点）。

### 5.1 主叙事一：LLM 自动特征工程 ≥ 人工特征工程（Load）

冷启动基线（time + lag_1/24，6 特征）vs 人工 full 特征集（15 特征）：

| 条件 | Load T15 best（3 seeds） | 是否反超人工 full (3.2993) |
|---|---|---|
| 人工 full | 3.2993 | — |
| Agent tier1（只加 lag） | 3.18 / 3.23 / 3.24 | ✅ 全部 |
| Agent tier3（加 cross） | 2.42 / 2.86 / 3.26 | ✅ 全部，均值 -13.7% |

> **结论**：LLM 从 6 个极简特征出发，自动重新发现滚动统计/周期交互，**稳定反超人工特征集**。

### 5.2 主叙事二：领域知识可插拔且可测（Solar）

| 档位 | 无先验 | 领域先验 | VAR169 使用率 |
|---|---|---|---|
| tier2 | -4.5% | **-8.0%** | 2/3 → 3/3 |
| tier3 | -4.9% | **-8.3%** | 0/3 → **3/3** |

> **结论**：一段"辐射是主导信号"的领域先验，让 LLM 开始用气象外生列，增益约翻倍——**先验的效果可量化验证**。

### 5.3 主叙事三：白名单 = 安全网（三数据集负对照）

| 数据集 | 正先验 | 无先验 | 负先验（错配/反事实） | 排序 |
|---|---|---|---|---|
| Wind | -3.5% | -3.0% | -1.9% / -2.3% | **正>无>负** ✓ |
| Solar | -8.3% | -8.7% | **-1.3%**（load 错配） | 无≈正 > 负 ✓ |
| Load | -4.3% | -12.4% | -13.0%（方差±14.7%） | 反转（白名单兜底） |

> **结论**：先验价值 = 指向列 × 信息占比；**错误先验/误导候选被白名单硬性拦截，系统结构上不会越界**。

### 5.4 多赛道 Agent 实验

| 赛道 | 实验 | 结果 |
|---|---|---|
| Price T15 | cold_start + price 先验 | -12.3% / -13.5%（2/3 seed 有效） |
| ECL 260 用户 | 记忆对照（Exp4） | Memory ≈ No-Memory（负面结果，见 5.5） |

### 5.5 ECL 记忆对照（诚实负面结果）

| 条件 | Mean RMSE | Median RMSE | 胜 persistence |
|---|---|---|---|
| No-Memory | 233.5 | 146.7 | 42.6% |
| Memory Agent（10 候选池） | 239.8 | 156.9 | 41.0% |

> **有价值的负面发现**：bootstrap 在 train 用户 val 上选优（`no_std24` 3427 < `base` 3433），但迁移到 61 个**完全未见用户**时收益被**分布偏移**抵消。**ECL 的正确载体是用户无关表示（PatchTST + RevIN，93.4% 胜 persistence），而非特征子集选择**。

### 5.6 测试与可复现性
9 个测试套件全绿（evolution E1-E20 / price P1-P6 / wind / solar / evaluation / lstm / uncertainty / ecl / ecl_memory），含负对照与多能源外循环。

---

## 六、创新点

1. **LLM-as-Decision-Maker 分工**：LLM 做特征工程决策、确定性引擎做安全执行——规避了 LLM 数值预测的不适配，同时获得 LLM 的语义理解与假设生成能力。
2. **受约束动作空间 + 白名单安全网**：三档递进的动作空间，外生列白名单 + 血缘静态检查，结构上杜绝越界与未来信息——经负对照实验证明。
3. **无泄漏闭环评测体系**：血缘静态检查（Pass A）+ 值级重算（Pass B）+ 别名扫描（Pass C）三重防护，所有结果跑在可审计的滚动回放上。
4. **领域知识可插拔 + 可测**：注册表式先验注入，配对照实验量化其价值边界（正/无/负三臂）。
5. **多能源统一框架**：Load/Solar/Wind/Price/ECL 五数据集统一接入，动作空间、评测协议、领域先验均按数据集配置。
6. **跨 Task 双闭环**：内循环单 Task 自进化 + 外循环漂移检测驱动策略迁移。
7. **诚实实验文化**：多 seed 报告均值±std、负面结果如实收录（ECL、Solar 正≈无）、bad case 单独分析。

---

## 七、Proposal / 未来工作

1. **决策价值主线（P-Value）**：把预测误差货币化为**储能套利利润 / Regret**——电价尖峰时段 1% 的 RMSE 改善可能对应利润翻倍，从"预测准"走向"预测有用"。
2. **自适应先验**：基于漂移检测动态选择/让位领域先验，解决"固定先验跟不上数据漂移"。
3. **多 Agent 协同**：特征 Agent + 模型选择 Agent + 不确定性 Agent 协同（V3.0），针对不同序列特性选后端与输出协议。
4. **经验记忆跨数据集迁移**：改进 ECL 记忆对照，聚焦用户无关特征（相对量/归一化）而非绝对 lag。
5. **概率预测收尾**：不确定性 Agent 的分位数输出接入决策评估，覆盖电价尖峰场景。

---

## 八、应用场景

1. **电网负荷预测**：调度计划、供需平衡、需求响应——Agent 自动特征工程替代人工试错。
2. **新能源并网**：光伏（辐射外生主导）/风电（风速外生）出力预测——领域先验注入直接提升外生特征利用。
3. **电力市场交易**：电价预测 + 储能套利（P-Value）——预测精度转化为真金白银。
4. **跨用户经验迁移**：新装电表、新小区负荷预测（ECL 范式）——用户无关表示迁移。
5. **可审计的 AutoML 服务**：无泄漏保证 + 全程审计，适合对预测可靠性/合规性要求高的工业场景。

---

## 附：关键文件索引

- 实验总结：[EXPERIMENTS_SUMMARY.md](EXPERIMENTS_SUMMARY.md)
- 代码：`agent/`（feature_spec / evolution_runner / domain_knowledge / uncertainty_agent）
- 评测：`evaluation/`（rolling_backtest / spec_evaluator / price_replay / ecl_protocol）
- 数据接入：`data/`（load/solar/wind/price/ecl 各自 loader + task_builder）
- 实验入口：`experiments/`（run_self_evolving_agent / run_outer_loop / run_price_replay / run_ecl_memory）
